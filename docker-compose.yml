version: '3.9'

services:

  backend:
    build: ./backend
    container_name: voting-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: voting-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
